# Evolution of Ioiô experience, 22 September 2026

Gate PASS. Confirmed: existing artist portfolio and WhatsApp conversion; same audience and contacts; retain Barlow typography. Scope: drag on both infinite portfolio tracks, one outlined ink word per heading, remove supplied footer symbol, profile photograph as social preview and secondary artist portrait, supplied mobile intro video with fading exit, a suspended ink-filled yo-yo in the hero and an S-shaped string motif in artist section. Existing hosting and deployment authorization retained.

Content inspected: both supplied video files are byte-identical: 1080x1920, 8 seconds, 6,969,098 bytes, H.264 with stereo AAC. To meet the shorter-intro request, the first 6 seconds were converted locally to H.264 540x960 with AAC audio and faststart, 1,123,544 bytes. Profile is 150x150: display small; social JPEG enlarged only for crawler compatibility, no invented detail. No new business claims.

Important assumptions: mobile means viewport below 700px; intro on each fresh load, including reload. Playback first attempts audio; browser rejection falls back to muted autoplay with an activate-sound control. Fade begins during the last 0.85 seconds and removes the video layer at `ended`; the WebGL ink already moves underneath, so the closing frame cannot remain over the hero. Skip control, error/timeout exit, no intro with reduced motion or save-data. WhatsApp may retain cached previews; metadata verification is not a test inside WhatsApp.

Resources selected: native Pointer Events and current CSS animations for drag; Canvas for live title masks and yo-yo ink; original SVG for S-shaped string only; local ffmpeg for video. No new production dependencies, paid services or 3D asset required. Motion best practices used for transform-based motion and reduced-motion handling. Reference: supplied animation is primary; Codrops Liquid Distortion for continuous texture, MDN autoplay guidance for graceful playback failure. Browser bridge remained unavailable (untrusted runtime); verification uses local Chrome with emulated mobile sizes.
